export const TRANSACTION_FORMAT = 2

/**
 * The aim of this client is to support data-related functionality ONLY;
 * therefore, token transfers aren't supported and relevant fields like
 * quantity and target are hard-coded for convenience
 */

export const TRANSACTION_QUANTITY = 0
export const TRANSACTION_TARGET = new Uint8Array()
